image: gradle:6.5.1-jdk8

stages:
  - build
  - test

build:
  stage: build
  script:
    - gradle clean assemble
  artifacts:
    paths:
      - build/libs/*.jar

test:
  stage: test
  script:
    - gradle test
