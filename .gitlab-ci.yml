image: raatiniemi/gradle:latest

stages:
  - build
  - test

build:
  stage: build
  script:
    - bin/build
  artifacts:
    paths:
      - build/libs/*.jar

test:
  stage: test
  script:
    - bin/test

sonarqube:
  stage: test
  script:
    - bin/sonar
